import { Login } from './index';

export const Routes = [
    {
        props: {
            component: Login,
            path: '/',
            exact: true
        },
        title: 'Login'
    }
];

